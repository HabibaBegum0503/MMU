//Excercise 1
// A simple drawing program

//float x; //declaring x as a float (decimal) varibale
//x = 50;
//println(x); // outputting x

//screen size 500x500 pixels
//size(500,500); //setting the canvas size (width,height)
//point(0,0); // placing a point at the coordinates
//ellipse(x,250,40,40); // (x,250) is the point and (40,40) is the width, height


// 1
// Make the circle appear in the middle of the screen (alter the value of variable x).

//float y; 
//y = 250;


//size(500,500); 
//point(0,0); 
//ellipse(y,250,40,40); 


// Make the circle appear in the bottom right corner of the screen (we should only see ¼ of it)
float z;
z = 500;
size(500,500);
point(0,0);
ellipse(z,500,40,40);

//2
// Make the circle double its original size
//float a;
//a = 50;
//size(500,500);
//point(0,0);
//ellipse(z,250,80,80);

//  ½ its original size
//float b;
//b = 50;
//size(500,500);
//point(0,0);
//ellipse(b,250,20,20);
